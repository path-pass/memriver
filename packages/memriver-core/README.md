# memriver-core

Core memory model, application logic, and SQLite-backed store for memriver: a
memory and project store with a deterministic, LLM-free write gate. Consumed
by the `memriver` MCP server; not a standalone tool.
