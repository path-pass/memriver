# memriver-core

Core memory model, application logic, and filesystem store for memriver: a
markdown memory store with a deterministic, LLM-free write gate. Consumed by
the `memriver` MCP server; not a standalone tool.
